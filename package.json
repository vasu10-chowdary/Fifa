{
  "name": "pulseos-fifa-wc2026",
  "version": "3.0.0",
  "description": "PulseOS — AI Operating System for FIFA World Cup 2026 Smart Stadiums",
  "main": "index.html",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --ci --coverage --reporters=default",
    "lint": "eslint js/**/*.js security/**/*.js",
    "lint:fix": "eslint js/**/*.js security/**/*.js --fix",
    "format": "prettier --write \"**/*.{js,css,html}\""
  },
  "keywords": ["fifa", "world-cup-2026", "stadium", "ai", "smart-stadium", "pulseos", "crowd-intelligence"],
  "author": "PulseOS Team",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.4",
    "@testing-library/user-event": "^14.5.1",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.0.3"
  }
}
